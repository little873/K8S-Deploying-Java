spring:
  datasource:
    url: jdbc:h2:mem:k8s_demo;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate

app:
  instance:
    pod-name: test-pod
    pod-ip: 10.244.1.20
    node-name: test-node
